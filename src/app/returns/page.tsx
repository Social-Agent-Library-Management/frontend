import type { Metadata } from "next";

import { PagePlaceholder } from "@/components/ui/page-placeholder";

export const metadata: Metadata = { title: "반납 처리" };

export default function ReturnPage() {
  return <PagePlaceholder title="반납 처리" />;
}
